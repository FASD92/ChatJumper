# Use a static Product Site

ChatJumper's Product Site will live in the same repository under `site/` and start as static HTML/CSS pages for the home page, privacy policy, and support page. A static site aligns with the low-cost launch goal, avoids adding a backend or separate site repository, and keeps GitHub Pages deployment simple; richer site tooling can be added later if the content outgrows this structure.

