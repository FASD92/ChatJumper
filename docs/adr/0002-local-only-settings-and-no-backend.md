# Use local-only settings and no backend

ChatJumper will store user preferences only in the current Chrome profile and will not include a backend, account system, analytics, license checks, or server-side synchronization for the first launch. This keeps the extension privacy-first, minimizes Chrome Web Store review complexity, and avoids infrastructure cost; the trade-off is that settings do not sync across devices.

